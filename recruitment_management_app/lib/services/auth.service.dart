import 'dart:async';
import 'dart:math';

// Tạm thời sẽ là fake auth => Sau này sẽ sửa sau TO-DO
class AuthService {
  // Login
  Future<bool> login() async {
    return true;
  }

  // Logout
  Future<void> logout() async {
    return true;
  }
}